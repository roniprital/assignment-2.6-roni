# assignment-2.6-roni
the client connects to a server (running on IP 127.0.0.1 and port 1729), sends commands in input entered by the user and displays the servers responses. the connection continues in a loop until user types EXIT.the server listens for client connections on port 1729, receives commands, acts on the requested action and sends back a response
